services:
  - type: web
    name: tradebricks
    env: static
    buildCommand: npm run build:render
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
